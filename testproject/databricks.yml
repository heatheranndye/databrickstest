# This is a Databricks asset bundle definition for testproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: testproject
  uuid: 3b556d4f-5530-4267-b838-5b589ca56acc

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-42c27cfa-ce08.cloud.databricks.com
    variables:
      catalog: samples
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-42c27cfa-ce08.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/heatheranndye@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/heatheranndye@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: samples
      schema: prod
    permissions:
      - user_name: heatheranndye@gmail.com
        level: CAN_MANAGE
